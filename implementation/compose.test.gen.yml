# Code generated by schemaf codegen. DO NOT EDIT.
# Test environment: ephemeral postgres (no volumes) + project services.
# Started/stopped by ./schemaf.sh test. Postgres data is discarded on stop.

name: rankedb-test

services:
  postgres:
    image: postgres:17-alpine
    container_name: rankedb-postgres-test
    environment:
      POSTGRES_USER: schemaf
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: rankedb
    ports:
      - "127.0.0.1::5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U schemaf -d rankedb"]
      interval: 2s
      timeout: 2s
      retries: 10
      start_period: 5s

networks:
  net:
    driver: bridge
    name: rankedb-test-net
