/**
 * @layer core
 * @description View mode and graph mode types.
 * @depends (none)
 * @must-not Import from ui/ or graph/. Reference React or DOM.
 */

export type ViewMode = 'timeline' | 'graph';
export type GraphMode = 'provenance' | 'semantic';
