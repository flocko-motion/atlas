# Code generated by schemaf codegen. DO NOT EDIT.
# Dev overrides: exposes service ports to localhost for local development.
# Run via: ./schemaf.sh dev

include:
  - compose/minio.dev.yml

services:
  postgres:
    environment:
      POSTGRES_PASSWORD: ${DB_PASS:-dev}
    ports:
      - "127.0.0.1:8003:5432"
